name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
     -name:Print Hello world
       run :echo "Hello world"
      -name:Print goodbye
        run:echo "Byeeee"
